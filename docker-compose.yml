services:
  api:
    image: composer:latest
    container_name: filme-api-container
    ports:
      - "8088:8000"
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: php artisan serve --host=0.0.0.0 --port=8000
